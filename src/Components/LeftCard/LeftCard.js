import React from 'react'
import { Strong, Text, Avatar, TickCircleIcon, minorScale, majorScale, pane, Button } from 'evergreen-ui';
import './logo.jpg'

export class LeftCard extends React.Component {
    render() {
        return (<div>
                    <pane is="section" background="yellowTint" border="muted" marginLeft={12} marginY={24} paddingTop={12} paddingX={40} width={120} height={120} cursor="help" onClick={() => alert('Works just like expected')}>
                
                    </pane>

                    
                </div>)}};